Labeled input. Use `mono` for page ranges and technical values; `error` for localized validation messages.

```jsx
<Input label="Rentang halaman" mono placeholder="1-3, 5, 8-10" hint="Pisahkan dengan koma" />
```
