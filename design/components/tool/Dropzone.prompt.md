Upload area for every tool page. Dashed border is reserved for drop zones. Always keyboard-accessible via the "Pilih file" button. Place a `<PrivacyPill>` directly above it.

```jsx
<Dropzone lang="id" multiple accept="PDF" onSelect={handle} />
```
