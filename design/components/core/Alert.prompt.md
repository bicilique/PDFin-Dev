Inline notice for warnings ("File besar dapat lambat diproses"), errors (localized error codes), success and info. Never show raw stack traces.

```jsx
<Alert tone="warning" title="File besar">File di atas 50 MB dapat lambat diproses di perangkat ini.</Alert>
```
